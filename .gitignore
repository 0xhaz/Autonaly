# Internal planning documents — deliberately not published.
# These carry product strategy, monetisation, kill criteria and competitive
# analysis. The public-facing equivalents are README.md and METHODOLOGY.md.
# Planning and demo material — never published. Globbed rather than named:
# these were exact filenames until 'architecture-complimesh-v2.md' slipped
# past them into a public repo, and 'demo-script copy.md' was next.
architecture*.md
hackathon*.md
techstacks*.md
workplan*.md
demo-script*.md
devpost-*.md
autonaly-thumbnail.png
autonaly-architecture.png
scripts/demo_reset*.py
article/

# secrets / env
.env
.env.gcp
*.pem
*-key.json
service-account*.json

# data + artifacts (never committed — see workplan.md §2)
data/
artifacts/
!artifacts/.gitkeep

# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
*.egg-info/
dist/
build/

# node
node_modules/
# copied from node_modules by a prebuild step (see web/package.json)
web/public/vendor/
.next/
out/
.vercel

# emulator state
.emulator/
firestore-debug.log
pubsub-debug.log

# os
.DS_Store

# clerk CLI link state
.clerk/
